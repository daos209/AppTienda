buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.1'
    }
}

apply plugin: 'base' // Agrega esta línea para incluir el plugin base que proporciona la tarea 'clean'

allprojects {
    repositories {
        google()
        jcenter()
        mavenCentral()
    }
}